import React, { useEffect } from "react";

const Tickets = () => {
  useEffect(() => {
    first;

    return () => {
      second;
    };
  }, [third]);

  return <div>Tickets</div>;
};

export default Tickets;
